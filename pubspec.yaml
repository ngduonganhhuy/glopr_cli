name: holmes_cli
description: A CLI to generate Flutter starter project from template. Built by Holmes <3
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  ansi_styles: ^0.3.2+1
  args: ^2.7.0
  io: ^1.0.5
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^4.0.0
  test: ^1.26.3
